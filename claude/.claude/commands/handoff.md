---
description: Write a session handoff doc so another developer (or you, in a different repo) can pick up where this session left off
argument-hint: "[optional note on what's next or who this is for]"
---

Load the `handoff` skill, then write a handoff for the current session. $ARGUMENTS

Report the file path, and warn if `.claude/handoffs/` is gitignored in this repo.
